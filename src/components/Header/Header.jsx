import React from 'react';

import NavItem from './NavItem/NavItem';

const header = (props) => {

  return (
    <header>
      <nav>
        <NavItem name="Home">
        </NavItem>
      </nav>
    </header>
  )
}

export default header;
